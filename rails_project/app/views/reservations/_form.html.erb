<%= simple_form_for([book, reservation]) do |f| %>
    <%= f.error_notification %>

  <div class="col-md-4">
    <div class="row">
      <div class="form-group col-md-12">
        <%= f.input :date_fin, class: 'form-control datepicker', :required => :true, :value => I18n.localize(Date.current.to_date, :format => :long) %>
      </div>
    </div>
    <div class="row">
      <div class="form-actions col-md-12">
        <%= f.button :submit, 'Réserver', class: 'btn btn-primary' %>
        <%= link_to 'Voir la fiche du livre', @book, class: 'btn btn-default' %>
      </div>
    </div>
  </div>

    <script type="text/javascript">
        $(document).ready(function(){
            $('.datepicker').datepicker({
                format: "dd MM yyyy",
                language: "fr"
            });
        });
    </script>

<% end %>


